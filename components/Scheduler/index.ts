/**
 * Public entry point for the scheduler UI. AppShell imports from here so
 * the old single-file `SchedulerModal` is no longer the source of truth.
 */

export { SchedulerModal } from "./SchedulerModal";
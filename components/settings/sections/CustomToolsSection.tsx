// Custom tool controls moved to the Tool Market. This empty module is retained
// temporarily so imports from older extensions do not fail at build time.
export function CustomToolsSection() {
  return null;
}

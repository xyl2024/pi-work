# Always install devDependencies, even when the shell has NODE_ENV=production.
# Without this, `npm install` skips devDependencies (typescript/tailwindcss/
# eslint/@types/*) under NODE_ENV=production, which breaks `next build` because
# the `@/*` tsconfig path alias is never registered. See AGENTS.md.
production=false